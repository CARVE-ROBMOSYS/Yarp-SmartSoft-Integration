//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _PARAMETERSTATESTRUCTCORE_HH
#define _PARAMETERSTATESTRUCTCORE_HH

#include "aceSmartSoft.hh"

#include <iostream>

// forward declaration (in order to define validateCOMMIT(ParameterStateStruct) which is implemented in derived class)
class ParameterStateStruct;

class ParameterStateStructCore
{
	friend class ParamUpdateHandler;
public:
	
		///////////////////////////////////////////
		// Internal params
		///////////////////////////////////////////
		
		/**
		 * Definition of Parameter Settings
		 */
		class SettingsType 
		{
			friend class ParamUpdateHandler;
		protected:
			/**
			 * here are the member definitions
			 */
			std::string configFile;
			std::string tickManager_localPort;
		
		public:
			// default constructor
			SettingsType() {
				configFile = "config.ini";
				tickManager_localPort = "/tickManager";
			}
		
			/**
			 * here are the public getters
			 */
			inline std::string getConfigFile() const { return configFile; }
			inline std::string getTickManager_localPort() const { return tickManager_localPort; }
			
			void to_ostream(std::ostream &os = std::cout) const
			{
				os << "Settings(";
				os << "configFile = " << configFile << ", ";
				os << "tickManager_localPort = " << tickManager_localPort << ", ";
				os << ")\n";
			}
			
		}; // end class SettingsType
		
	
		///////////////////////////////////////////
		// External params
		///////////////////////////////////////////
		
	
		///////////////////////////////////////////
		// Instance params
		///////////////////////////////////////////
		
		/**
		 * Definition of instantiated ParameterRepository CommYARP_BT
		 */
		class CommYARP_BTType {
			friend class ParamUpdateHandler;
			public:
			/**
			 * Definition of instantiated ParameterSet ConfigParams
			 */
			class ConfigParamsType {
				friend class ParamUpdateHandler;
				public:
				protected:
					/**
					 * internal members
					 */
					
				public:
					/**
					 * public getter methods
					 */
					
					void to_ostream(std::ostream &os = std::cout) const
					{
						os << "ConfigParams(\n";
						os << ")";
					}
			}; // end of parameter-set class ConfigParamsType
			
			protected:
				/**
				 * internal members
				 */
				ConfigParamsType ConfigParams;
			
			public:
				/**
				 * public getter methods
				 */
				inline ConfigParamsType getConfigParams() const { return ConfigParams; }
				
				void to_ostream(std::ostream &os = std::cout) const
				{
					os << "CommYARP_BT(\n";
					ConfigParams.to_ostream(os);
					os << ")";
				}
		}; // end of parameter-repository wrapper class CommYARP_BTType
	
protected:

	// Internal params
	SettingsType Settings;
	
	// External params
	
	// Instance params (encapsulated in a wrapper class for each instantiated parameter repository)
	CommYARP_BTType CommYARP_BT;
	

	void setContent(const ParameterStateStructCore &commit) {
		// External params
	
		this->CommYARP_BT = commit.getCommYARP_BT();
	}

	// special trigger method (user upcall) called before updating parameter global state
	virtual SmartACE::ParamResponseType handleCOMMIT(const ParameterStateStruct &commitState) = 0;
public:
	ParameterStateStructCore() {  }
	virtual ~ParameterStateStructCore() {  }
	
	// internal param getters
	SettingsType getSettings() const {
		return Settings;
	}
	
	// external param getters
	
	// repo wrapper class getter(s)
	CommYARP_BTType getCommYARP_BT() const {
		return CommYARP_BT;
	}
	
	// helper method to easily implement output stream in derived classes
	void to_ostream(std::ostream &os = std::cout) const
	{
		// Internal params
		Settings.to_ostream(os);
		
		// External params
		
		// Instance params (encapsulated in a wrapper class for each instantiated parameter repository)
		CommYARP_BT.to_ostream(os);
	}
};

#endif
